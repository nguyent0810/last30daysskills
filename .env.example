# Neon Postgres (or any Postgres URL for `pg`)
DATABASE_URL=

# Required for signed anonymous session cookies (min 16 chars)
SESSION_SECRET=

# Optional: worker poll interval (ms)
# WORKER_POLL_MS=3000

# Optional: GitHub PAT for `git push` from this machine only (never commit)
# GITHUB_TOKEN=
