# Neon Postgres connection string (pooled or direct)
DATABASE_URL=

# Optional: worker poll interval (ms)
# WORKER_POLL_MS=3000

# Optional: GitHub PAT for `git push` from this machine only (never commit; prefer GitHub CLI or credential manager)
# GITHUB_TOKEN=
